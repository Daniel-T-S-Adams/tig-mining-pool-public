[package]
name = "pool-telemetry"
description = "Structured logging setup shared by pool binaries (docs/architecture.md §10.1)"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
version = "0.1.0"

[dependencies]
pool-config = { path = "../pool-config" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

[lints]
workspace = true

[dev-dependencies]
serde_json = "1"
